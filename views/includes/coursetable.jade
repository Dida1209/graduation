//
   Created by lenovo-pc on 2017/2/25.

.coursetable
    .courseName #{resource.title}
    .courseContainer
        .courseDetail
            span 内容类型
            p #{resource.type}
        .courseDetail
            span 课程分类
            p #{resource.subjection}
        .courseDetail
            span 课程简介
            p #{resource.summary}
    .courseBottom
        .bottom
            a.zan #{resource.likeNum}
            a.comment #{resource.commentNum}
            a.collect #{resource.collectionNum}